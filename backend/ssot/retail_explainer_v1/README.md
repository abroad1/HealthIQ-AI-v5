# `retail_explainer_v1` SSOT

Governed location for **retail / educational** copy keyed by biomarker id and cluster-schema system key.

- **Registry:** `registry.yaml` — human-edited; validated at load time.
- **Schema:** see `core/contracts/retail_explainer_v1.py` and `RETAIL_EXPLAINER_CONTENT_BOUNDARIES_v1.md`.

Population is **B1B+**; B1A ships **schema + pilot sample** only.
