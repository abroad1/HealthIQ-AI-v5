# Local / CI / Docker: pip install -r requirements.txt
# Canonical dependency declarations: pyproject.toml [project.dependencies] — keep these aligned.
# Direct imports: gotrue (core.dependencies.auth, app.routes.auth), supabase (core.supabase_anon), sqlalchemy (config.database).

fpdf2>=2.7.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
google-generativeai>=0.5.0
gotrue>=2.0,<3
supabase>=2.4.0
sqlalchemy>=2.0.0
python-dotenv>=1.0.0
psycopg2-binary>=2.9.0
requests>=2.31.0
PyYAML>=6.0.0

# Dev / test (used by CI workflows and local pytest)
mypy
ruff
pytest
pytest-asyncio
pytest-html
pytest-json-report
alembic
selenium
