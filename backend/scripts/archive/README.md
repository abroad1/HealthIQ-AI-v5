# Archived Scripts

Scripts archived after Sprint 18 AB panel migration finalisation (staging folder removed).

- `migrate_ab_staging_to_runtime.py` — Migration script; staging merged into runtime, folder removed.
- `report_ab_panel_delta.py` — Delta report; required staging folder.
