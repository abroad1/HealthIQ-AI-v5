name: CI

on:
  push:
    branches: ['*']
  pull_request:
    branches: ['*']

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Cache pip
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('backend/pyproject.toml') }}

      - name: Upgrade pip
        run: pip install -U pip

      - name: Install backend dependencies
        run: pip install -e ./backend

      - name: Check ruff version
        run: python -m ruff --version || pip install ruff

      - name: Check mypy version
        run: python -m mypy --version || pip install mypy

      - name: Run ruff
        run: python -m ruff check backend --select E9,F63,F7,F82 --ignore-noqa --isolated

      - name: Run mypy
        run: python -m mypy backend

      - name: Run pytest
        run: pytest -q backend

  frontend:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache npm
        uses: actions/cache@v3
        with:
          path: frontend/node_modules
          key: ${{ runner.os }}-npm-${{ hashFiles('frontend/package-lock.json') }}

      - name: Install frontend dependencies
        run: npm ci
        working-directory: frontend

      - name: Build frontend
        run: npm run build
        working-directory: frontend
