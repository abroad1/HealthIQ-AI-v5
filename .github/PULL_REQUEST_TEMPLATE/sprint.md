### Scope

(What changed and why — keep to one or two sentences. Compute-only unless explicitly enabling a flag.)



### Guarantees

- No fallback/dummy parsers

- Additive only; feature flags OFF by default

- No live routing changes (unless this PR explicitly enables a flag)



### CI

- Validator ✅ / Smokes ✅ / No-fallback grep ✅ (see GitHub checks below)



### Notes

(Optional reviewer notes. Do **not** paste logs; CI artifacts provide evidence.)

