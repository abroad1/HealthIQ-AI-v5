{
  "bus_version": "1.3.1",
  "work_id": "ARCH-CONV-FINAL-AUDIT",
  "status": "HARDENED",
  "hardening_date": "2026-07-26",
  "hardened_by": "Claude Code",
  "source_prompt": "automation_bus/latest_cursor_prompt.md",
  "risk_level": "HIGH",
  "change_type": "CONTENT",
  "execution_model": "TWO_PHASE_START_FINISH",
  "stage_b_mode": "MODE_2",
  "runtime_change": "NONE",

  "verification_mode_used": {
    "mode": "batched parallel calls, no fork for hardening itself",
    "rationale": "Hardening a prompt (verifying its premises and authority paths) is a different task from executing it. This prompt explicitly authorizes/expects the EXECUTING agent to use disjoint parallel review agents across its 5 workstreams — that is correctly Cursor's/the auditor's decision at execution time, not something to pre-empt during hardening. For hardening itself, I already hold exhaustive, independently-verified context on PKG1/PKG2/PKG3 from this same session (including live code execution and byte-identical hash checks), so batched direct checks were sufficient to verify this prompt's premises."
  },

  "prompt_receipt_gate": {
    "front_matter_complete": "YES",
    "fields_present": ["work_id", "branch", "risk_level", "execution_model", "change_type", "stage_b_mode", "runtime_change"],
    "declared_risk_level": "HIGH",
    "declared_change_type": "CONTENT",
    "behaviour_changes_touch_intelligence_core": "NO — runtime_change: NONE; this is a read-only + test-execution + human-UAT audit, not a code change. HIGH risk_level is appropriate given the safety-critical stakes of confirming no leaked medical logic, not because code is being modified.",
    "tests_required": "NOT run-time-BEHAVIOUR tests in the schema sense (CONTENT/NONE), but extensive re-execution of existing test suites and new end-to-end scenarios is mandated as audit evidence — treat as required verification method, not a runtime change.",
    "scope_proportionality": "PASS — this is explicitly the final, closing audit for a 3-package programme plus mandatory human UAT; its unusually large scope (5 workstreams, full pipeline inspection, human UAT gate, kill-criteria review) is proportionate to being the terminal programme decision, not scope creep.",
    "advisory_duplication_detected": "NO",
    "scope_creep_signals": "NONE beyond what a final closing audit legitimately requires; the prompt itself explicitly authorizes disjoint parallel review agents (workstreams 1-5), which is expected and appropriate at this scale.",
    "decision": "ACCEPT"
  },

  "stage_2a_authority_verification": {
    "required_input_docs": {
      "status": "CONFIRMED_EXISTS — all ~15 named/globbed docs",
      "evidence": "Planning paper; CONTROLLED_BETA_ARCHITECTURE_COHORT.md; CONVERGENCE_VIABILITY_ASSESSMENT.md; WHY_MIGRATION_PILOT_COHORT.md; WHY_PILOT_MEDICAL_REVIEW_READINESS.md; both PKG1 docs; both PKG2 docs + verification report; all four PKG3 docs + the GPT-reviewed/Anthony-ratified pack + verification report; ADR-RT-IDENTITY-PROV-001; BUILD_DELIVERABLE_REGISTER.md — all confirmed present via direct Glob/prior-session confirmation."
    },
    "output_paths_correctly_new": {
      "status": "CONFIRMED",
      "evidence": "None of the 5 required ARCH-CONV-FINAL-AUDIT deliverable paths exist yet."
    },
    "uat_folder_convention_mismatch": {
      "status": "FLAGGED — not blocking, low-confidence correction (thin precedent)",
      "evidence": "The prompt targets docs/uat/ARCH-CONV-FINAL_frontend_end_to_end_uat.md; no docs/uat/ directory currently exists. The one existing historical precedent is docs/testing/UAT1/UAT1_findings.md (referenced in AUTHORITY_MAP.md as ARCHIVE, 'date uncertain'). Unlike the Medical Research Documents convention (6 real files, high confidence), this precedent is thin (one old file) — recommend the executing auditor check with the actual repository convention before creating a new top-level docs/uat/ directory, but this is a much softer correction than the medical-research folder finding at GATE2_5."
    },
    "branch_state": {
      "status": "CONFIRMED, expected pre-kernel-start state",
      "evidence": "Current branch is main (HEAD includes 2ecb02a, merge of 'ARCH-CONV-PKG3 WHY authority migration (GO)' — confirms PKG3 was human-merged). Target branch audit/arch-conv-final-programme does not yet exist. Working tree shows only the expected latest_cursor_prompt.md Stage-1 handoff modification."
    }
  },

  "independent_premise_verification": {
    "rejected_frame_identity_matches_prior_independent_verification": {
      "status": "CONFIRMED",
      "evidence": "This prompt's Workstream 3 and scenario 1 both name signal_homocysteine_high::inv_homocysteine_high_metabolic as the rejected frame requiring live-execution proof of inertness. This exactly matches what this session's own PKG3 audit already live-executed and confirmed (resolve_frame_why_authority -> mode='skip'; compile_root_cause_v1() -> root=None). The final audit should re-run this itself at its own baseline SHA rather than citing the prior session's result, per the independence requirement below, but the premise itself (which frame, which activation_key) is accurate."
    },
    "nine_compiled_active_plus_one_rejected_matches_register": {
      "status": "CONFIRMED",
      "evidence": "Matches the compiled_why_authority_register_v1.yaml content independently verified during the PKG3 audit (10 entries: 9 COMPILED_ACTIVE, 1 REJECTED with artefact_path: null)."
    },
    "mandatory_human_UAT_stop_is_correctly_designed_as_a_kernel_boundary": {
      "status": "CONFIRMED as correctly structured, not a gap to fix",
      "evidence": "This prompt explicitly states: 'Complete the automated and code-based audit first, then STOP and hand the UAT plan to Anthony... After Anthony's evidence is added, resume the same audit work ID and complete the programme decision.' This is exactly the two-kernel-run structure that PKG3's mandatory Gate C required (and which this session's hardening pass for PKG3 had to infer/recommend, since that prompt did not spell it out as explicitly). This prompt gets it right by design — a positive observation, not a finding requiring correction."
    },
    "independence_requirement_interpretation": {
      "status": "MATERIAL CLARIFICATION for the executing auditor",
      "evidence": "This prompt states 'Do not rely on implementation reports or prior PASS decisions as proof... Re-execute material tests, inspect live code.' This session's Claude Code audits of PKG1/PKG2/PKG3 already did exactly this (live code execution, byte-identical hash verification, direct test re-runs) — those are genuine independent audit artefacts, not mere 'implementation reports.' However, the safest and most defensible reading of this prompt's independence requirement is that the FINAL audit should still perform its OWN fresh re-execution at its own baseline SHA (cheap, removes any doubt about drift between then and now) rather than citing prior Claude Code audit summaries as a substitute for re-verification. Recommend treating prior audit summaries as corroborating context to compare against, not as evidence itself."
    }
  },

  "non_blocking_observations_for_cursor": [
    "Workstreams 1-5's disjoint-agent structure is explicitly requested by the prompt itself — at execution time, size each workstream's agent scope to avoid overlap (e.g., Workstream 3's medical-fidelity check and Workstream 4's end-to-end scenarios both touch the same 10 pilot frames from different angles; make sure the two agents compare notes rather than silently duplicating or silently disagreeing).",
    "For Workstream 5's legacy-fingerprint scan, recommend building the fingerprint set FROM the actual retired-legacy YAML content and the actual rejected frame's hypothesis text (both directly readable from this session's PKG3 evidence) rather than re-deriving it from scratch, to ensure the fingerprint strings are exact, not paraphrased approximations that could miss a real leak.",
    "The UAT folder path convention (see authority verification above) should be confirmed against actual repository practice before creating a new top-level directory."
  ],

  "stage_2c_evidence_checklist": {
    "checklist_count": 6,
    "all_confirmed": true,
    "correction_required": false,
    "items": [
      {"id": 1, "claim": "All ~15 required input docs (named + globbed) exist", "source": "Glob per path, prior-session confirmation", "evidence": "All confirmed present", "status": "CONFIRMED"},
      {"id": 2, "claim": "All 5 required output paths are correctly new", "source": "Glob per path", "evidence": "None of the 5 deliverables exist yet", "status": "CONFIRMED"},
      {"id": 3, "claim": "Branch state expected; PKG3 merged", "source": "git log; git branch --list; git status --short", "evidence": "HEAD includes 2ecb02a, the PKG3 merge commit; target branch not yet created; only latest_cursor_prompt.md modified", "status": "CONFIRMED"},
      {"id": 4, "claim": "No docs/uat/ directory exists; actual (thin) precedent is docs/testing/UAT1/", "source": "Glob", "evidence": "docs/uat/* empty; docs/testing/UAT1/UAT1_findings.md exists (AUTHORITY_MAP: ARCHIVE, date uncertain)", "status": "CONFIRMED"},
      {"id": 5, "claim": "Rejected frame identity (signal_homocysteine_high::inv_homocysteine_high_metabolic) and 9/1 COMPILED_ACTIVE/REJECTED split match the actual register", "source": "This session's own PKG3 audit, independently live-executed", "evidence": "Exact match", "status": "CONFIRMED"},
      {"id": 6, "claim": "The mandatory human-UAT STOP is correctly designed as a two-kernel-run boundary, not an in-run pause", "source": "Direct read of the prompt's own 'resume the same audit work ID' language", "evidence": "Matches the mechanically-necessary pattern already observed working correctly in PKG3 (Gate C)", "status": "CONFIRMED"}
    ]
  },

  "rejection_conditions": [
    "Any substantive correction implemented inside this audit package (forbidden — corrections must be recommended as CORRECT/STOP/V6, not applied here) — REJECT_AND_RETURN",
    "A final PASS issued before Anthony's UAT evidence is actually present — REJECT_AND_RETURN",
    "Any reliance on Cursor's own prior implementation reports, or on this session's prior Claude Code audit summaries, as a substitute for fresh re-execution at this audit's own baseline SHA — REJECT_AND_RETURN or treat as an audit-quality defect",
    "Any PASS issued while a safety-material item remains UNRESOLVED, or while any ACTIVE_LEAK or BOUNDARY_LEAK classification exists — REJECT_AND_RETURN",
    "Any beta-readiness declaration not separately and explicitly assessed — REJECT_AND_RETURN"
  ],

  "overall_verdict": "HARDENED. CLEARED FOR KERNEL START. All required input documents and all 5 required output paths confirmed; branch state confirmed expected pre-kernel-start with PKG3's GO outcome confirmed merged. The rejected-frame identity and 9/1 compiled/rejected split this prompt relies on were independently reconfirmed against this session's own live-executed PKG3 audit. The mandatory human-UAT STOP gate is correctly designed by this prompt as a genuine two-kernel-run boundary (matching the mechanically-necessary pattern already proven to work in PKG3's Gate C) rather than an impossible in-run pause -- a positive design observation, not a gap. One low-confidence, non-blocking path-convention note: the prompt's assumed docs/uat/ directory has no real precedent (only a thin, archived docs/testing/UAT1/ file) and the executing auditor should confirm the actual convention before creating a new top-level directory. One material clarification recorded for the executing auditor: this session's own prior Claude Code audits of PKG1/PKG2/PKG3, while genuinely independent, should still be treated as corroborating context rather than a substitute for this audit's own fresh re-execution at its own baseline SHA, consistent with the prompt's explicit independence requirement. No blocking defect found. Recommend proceeding to kernel start."
}
