{
  "bus_version": "1.3.1",
  "work_id": "ARCH-CONV-PKGC-1",
  "status": "HARDENED",
  "hardening_date": "2026-08-02",
  "hardened_by": "Claude Code",
  "source_prompt": "automation_bus/latest_cursor_prompt.md (front matter complete: work_id, branch feature/arch-conv-pkgc-1-waist-unit-remediation, risk_level STANDARD, execution_model TWO_PHASE_START_FINISH, change_type MIXED)",
  "risk_level": "STANDARD",
  "change_type": "MIXED",
  "execution_model": "TWO_PHASE_START_FINISH",

  "verification_mode_used": {
    "mode": "batched parallel calls, no fork",
    "rationale": "This work package directly implements the ARCH-CONV-PKGC-1 half of this session's own Stage 0 bundling revalidation (SPLIT INTO ARCH-CONV-PKGC-1, ARCH-CONV-PKGC-2). The governing sources (WAIST_UNIT_LEGACY_IMPACT_AUDIT.md, LAUNCH-CORE-3 policy, result_versioning_policy_v1.py, analysis_regeneration_v1.py) were already read in full in that immediately-prior Stage 0 pass. Remaining gaps (migration mechanism availability, schema audit-trail capacity, fresh branch/carry-forward state) closed with a small number of targeted parallel reads. No disjoint large-scale work requiring a fork."
  },

  "prompt_receipt_gate": {
    "front_matter_complete": "YES",
    "declared_risk_level": "STANDARD",
    "declared_change_type": "MIXED",
    "scope_proportionality": "PASS -- matches this session's own Stage 0 revalidation recommendation exactly (waist-unit stale-detection rule + governed 12-row remediation only); explicitly excludes ARCH-CONV-PKGC-2 (provenance-identity), the deferred regeneration job, CF-MEDREV2-002, and full result-versioning advancement",
    "advisory_duplication": "NO conflict -- direct implementation of this session's own split recommendation from the immediately-prior Stage 0 bundling revalidation",
    "phase0_only_scope_correctly_declared": "CONFIRMED -- prompt explicitly STOPs after committing the Phase 0 data-governance pack and before any historic-row mutation or rule addition (lines 189-200: 'Do not add the stale-detection rule... Do not mutate any historic analysis row... Do not run a remediation script in write mode'); Phase 1 heading (line 202) is explicitly gated on 'data-governance approval only', with a required Anthony (not Head of Medical Research) decision -- correctly reflecting that this is a data-governance disposition, not a medical-content Gate 1/Gate 2 decision (prompt line 54: 'No medical-content Gate 1 or Gate 2 is expected')",
    "decision": "ACCEPT"
  },

  "stage_2a_authority_verification": {
    "carry_forward_status": {
      "status": "CONFIRMED still Open",
      "evidence": "docs/sprints/launch_core_carry_forward_register.md:99 -- CF-ARCH-CONV-WAIST-1, status Open, '48 legacy bare waist rows audited; 12 classified used_incorrectly. Forward path fixed; historic disposition not executed.' Re-confirmed this session, unchanged since the prior Stage 0 pass."
    },
    "twelve_row_affected_set": {
      "status": "CONFIRMED",
      "evidence": "docs/audit-papers/WAIST_UNIT_LEGACY_IMPACT_AUDIT.md read in full in the immediately-prior Stage 0 pass: 48 legacy bare rows classified, 12 used_incorrectly (exact analysis_id, created_at, original_value, incorrect_mapped_cm_if_inches columns present for each), 36 dropped_as_implausible, 0 analysis_start_blocked. This audit table is the governed evidence source the prompt's own Phase 0 data-remediation register (lines 150-167) must be populated from."
    },
    "stale_detection_framework_and_gap": {
      "status": "CONFIRMED",
      "evidence": "backend/core/dto/result_versioning_policy_v1.py read in full (173 lines) in the immediately-prior Stage 0 pass and re-confirmed this session: detect_launch_core_stale_reasons() implements exactly six existing stale-reason rules (completeness_policy_mismatch/missing, card_subsystem_completeness_mismatch, legacy_hard_coded_subsystem_trace, legacy_total_bilirubin_false_missing). Grep for 'waist' across result_versioning_policy_v1.py, persisted_replay_contract_v1.py, and analysis_regeneration_v1.py: zero matches, this session -- the waist-unit rule genuinely does not exist yet."
    },
    "regeneration_job_and_dependencies_confirmed_absent": {
      "status": "CONFIRMED",
      "evidence": "backend/core/dto/analysis_regeneration_v1.py read in full: assess_regeneration_available() is a pure read-only boolean check (stored_raw_biomarkers_sufficient) -- no analysis-creation/regeneration job exists. This confirms Stage 1A item 11 -- the governed remap/stale-mark treatment this prompt scopes does not require the unbuilt regeneration job, CF-MEDREV2-002, a DB lineage table, or result-versioning redesign."
    },
    "schema_audit_trail_capacity": {
      "status": "CONFIRMED sufficient without new columns, migration path also available if needed",
      "evidence": "backend/core/models/database.py: Analysis.raw_biomarkers (JSON, line 117), Analysis.processing_metadata and AnalysisResult.processing_metadata (JSON, lines 179/371/503), AnalysisResult.result_version (String, line 177) -- flexible JSON columns can carry remediation action/reason/timestamp/actor/reversibility metadata without a new migration. backend/migrations/ (alembic) confirmed present and actively used (recent targeted migrations: add_replay_manifest_column.py, insight_provenance_columns.py, s7_profiles_billing_columns.py) -- the 'repository's established migration/remediation mechanism' the prompt requires preference for (line 255) genuinely exists."
    },
    "existing_test_surface": {
      "status": "CONFIRMED, disjoint from PKGC-2",
      "evidence": "backend/tests/regression/test_internal_uat_result_versioning_dto_contract.py, test_med_rev2_domain_card_copy_and_regeneration.py, backend/tests/unit/test_launch_core3_result_versioning.py -- confirmed via grep this session, zero overlap with the provenance test surface (test_output_authority_provenance.py, test_arch_rt_identity_prov_1.py), consistent with this session's Stage 0 split rationale."
    },
    "risk_classification_check": {
      "status": "STANDARD confirmed correct on current evidence, with an explicit caveat carried into the Phase 0 pack",
      "evidence": "result_versioning_policy_v1.py lives under backend/core/dto/, not backend/core/analytics/ or backend/core/pipeline/ -- it performs presentation/staleness metadata assessment over already-computed results, not signal evaluation, WHY compilation, ranking, or output construction. detect_launch_core_stale_reasons() reads persisted DTO shape only; it does not alter medical reasoning, ranking, interpretation, or output construction. This supports STANDARD, matching the prompt's own expected classification and its Stage 1A classification-check question. Flagged as CONFIRMED rather than merely inherited from the prior Stage 0 pass's provisional label, since this session traced the actual file path and function behaviour rather than assuming it."
    },
    "repo_state": {
      "status": "CONFIRMED",
      "evidence": "git branch --show-current: main; git rev-parse HEAD == git rev-parse origin/main: a433f9f6b846a82317f928048c316bc1d8a78b0c; git status --short: only automation_bus/latest_cursor_prompt.md modified (the prompt being hardened itself, expected); git stash list: empty."
    }
  },

  "stage_2b_behavioural_hardening": {
    "reality_check_1b": "Repository-confirmed TRUE as of this session for all Stage 1B premises: the 12 audit-listed analysis IDs are the governed evidence set (no re-verification of live DB state possible from static repo files -- this is an explicit Phase-0-only verification gap, see below); the stale-detection framework still lacks a waist-unit rule; the six-rule framework can represent a seventh rule without redesign (same function, same return-shape contract); remediation does not require regeneration (confirmed above); this is not a no-op sprint.",
    "genuine_hardening_limitation_disclosed": "This hardening pass cannot verify Stage 1A item 9 (whether any of the 12 rows has already been remediated, deleted, superseded, or regenerated) or the live 'current persisted value' for each row (Stage 1A item 5) from static repository files -- that requires direct database access, which is correctly scoped as Cursor's own Phase 0 repository-mapping responsibility, not something hardening can substitute for. This is disclosed rather than silently assumed.",
    "data_governance_boundary_correctly_distinct_from_medical_gate": "Confirmed the prompt correctly routes this to an Anthony-only data-governance decision (line 178-187), not a Head of Medical Research Gate 1 -- consistent with the register's own field for L-11/CF-ARCH-CONV-WAIST-1 ('medical-review requirement: No (data integrity)') read in the prior Stage 0 investigation.",
    "no_new_mechanism_required": "Confirmed achievable with existing mechanisms -- the stale-detection rule reuses the existing detect_launch_core_stale_reasons() contract and return shape (six precedent rules to follow); the remediation mechanism has a governed migration path available (alembic) if schema changes are needed, or the existing JSON processing_metadata columns if not."
  },

  "gaps_or_risks_flagged_for_gpt_and_anthony": [
    "This hardening pass could not independently verify live database state for the 12 analysis IDs (current values, whether already remediated) from static repository files. Cursor's own Stage 1A repository-mapping phase must perform this verification with real database access before the Phase 0 data-remediation register is populated -- this is a genuine, disclosed limit of hardening-stage verification, not an oversight.",
    "The prompt's permitted disposition set (GOVERNED_REMAP, MARK_STALE_NO_REWRITE, ALREADY_REMEDIATED_NO_ACTION, BLOCKED_AMBIGUOUS) has no guarantee all 12 rows will cleanly sort into these four buckets -- Anthony's data-governance approval should be prepared to receive a register where some rows are BLOCKED_AMBIGUOUS, and the prompt's own instruction (line 253: 'Do not claim all 12 were remediated if any remain blocked or no-action') and closure instruction (line 360: partial-closure handling) already anticipate this correctly.",
    "Risk classification (STANDARD) is confirmed correct on current evidence but is explicitly re-checked at Stage 1A by the prompt itself (line 52, 101-109) rather than inherited -- this hardening pass supports that re-check with a concrete file-path/function-behaviour trace rather than treating STANDARD as settled."
  ],

  "evidence_checklist_status": "COMPLETE -- see chat response for the mandatory Stage 2C table"
}
