{
  "bus_version": "1.3.1",
  "work_id": "V5-CANONICAL-ACTIVATION-GATE-1",
  "status": "HARDENED",
  "hardening_date": "2026-08-08",
  "hardened_by": "Claude Code",
  "risk_level": "HIGH",
  "change_type": "BEHAVIOUR",
  "execution_model": "TWO_PHASE_START_FINISH",
  "branch": "refactor/v5-canonical-activation-gate-1",
  "source_prompt": "automation_bus/latest_cursor_prompt.md",
  "prompt_sha256": "acc5ee3f26e2e343d8640c1acb6c296eba07187c1f3be56dcbf2eee4e1e12c24",

  "verification_mode_used": {
    "mode": "batched direct verification, no fork",
    "rationale": "Single bounded HIGH/BEHAVIOUR refactor package building directly on V5-RUNTIME-AUTHORITY-INTEGRITY-1's merged transition design; did not require disjoint parallel research."
  },

  "prompt_receipt_gate": {
    "front_matter_complete": "YES",
    "declared_risk_level": "HIGH",
    "declared_change_type": "BEHAVIOUR",
    "files_in_scope": "4 named authority files (Stage 1A) + 2 new artefacts + 1 updated document -- enumerated",
    "behaviour_changes_touch_intelligence_core": "YES -- SignalRegistry activation gating, consistent with declared HIGH",
    "tests_listed_for_behaviour_changes": "YES -- 8 canonical regression targets + exact-set determinism proof",
    "scope_proportionality": "PASS -- bounded, builds on a prior closed package's transition design, 9 STOP conditions",
    "advisory_duplication": "NO",
    "decision": "ACCEPT"
  },

  "stage_d_authority_verification": {
    "prior_package_merged": "V5-RUNTIME-AUTHORITY-INTEGRITY-1 confirmed merged to main at afa4dab",
    "authority_files_confirmed_current": [
      "docs/architecture/V5_RUNTIME_ACTIVATION_CANONICAL_GATE_TRANSITION.md",
      "knowledge_bus/governance/package_runtime_activation_register_v1.yaml",
      "backend/core/analytics/signal_evaluator.py",
      "backend/core/knowledge/runtime_medical_authority_integrity_v1.py"
    ],
    "stage_1b_reality_check": "CONFIRMED -- more than one mechanism can currently grant activation independently. Not a no-op."
  },

  "central_finding_flagged_for_cursor_stage_1a_1b": {
    "status": "CANDIDATE STOP-2 TRIGGER -- not pre-resolved, flagged for Cursor's explicit classification",
    "mechanism": "backend/core/knowledge/package_runtime_eligibility_v1.py:57-100 (classify_package_runtime_eligibility). Non-launch-critical packages (line 89-92) are gated by is_package_runtime_activated() -- genuinely consult the activation register, consistent with the transition document. Launch-critical packages (pkg_kb47_*) skip that branch entirely (line 89 condition false) and are eligible solely via is_beta_eligible_explicit_lineage(status) -- provenance/lineage quality. The activation register is never consulted for this cohort. Confirmed independently in SignalRegistry._load: line 139's 'if governed and not launch_critical' also skips the frame-level register check for launch-critical frames.",
    "why_this_matters": "package_runtime_eligibility_v1.py's own docstring (line 2) calls itself 'canonical package runtime eligibility (launch-critical cohort)' -- a second, self-described canonical authority, separately ratified under ARCH-CONV-PKG2, governing a disjoint high-stakes cohort (TSH/FT3/FT4/eGFR-class signals). The prior transition document classified this only as a 'supporting constraint to fold Later' -- read strictly against the current code, that undersells it: for launch-critical packages, eligibility IS the entire grant decision, not a veto on top of the register's grant.",
    "why_not_simply_foldable_this_sprint": "Folding would require either migrating ~150 pkg_kb47_* entries into the register (a 'broad package migration', explicitly named in STOP 9 as out of scope) or redefining what 'canonical' means per-cohort (new architecture policy, per STOP 4).",
    "proposed_narrowest_safe_resolution": "Scope this sprint's 'canonical gate' claim to the non-launch-critical cohort only, and explicitly document the launch-critical eligibility path as a second, disjoint-cohort canonical authority this sprint does not touch -- rather than falsely claiming one universal gate now, or attempting the launch-critical fold and risking STOP 9. A full merge of the two cohorts under one gate is the genuinely 'Later' work the prior transition document already deferred.",
    "instruction_to_cursor": "This is a proposal, not a directive. Cursor's Stage 1A item 5 classification must make this call explicitly and STOP under condition 2 if it disagrees that the disjoint-cohort framing is safe, rather than silently proceeding either way."
  },

  "other_findings": {
    "governed_bypass": "backend/core/analytics/signal_evaluator.py:82 -- packages outside knowledge_bus/packages/ (e.g. test harness fixtures) bypass activation-register gating entirely, per a pre-existing documented comment. Narrower concern than the launch-critical finding, confined to non-estate test fixtures, unlikely to be STOP-relevant alone but worth Cursor citing in the pre-change map."
  },

  "canonical_artefacts_to_produce": [
    "docs/architecture/V5_CANONICAL_ACTIVATION_GATE_prechange_map.md",
    "backend/tests/architecture/test_canonical_runtime_activation_gate.py",
    "updated/superseded docs/architecture/V5_RUNTIME_ACTIVATION_CANONICAL_GATE_TRANSITION.md"
  ],

  "evidence_checklist_status": "COMPLETE -- prior package confirmed merged, all 4 authority files verified current, Stage 1B reality check confirmed (not a no-op), central STOP-2-candidate finding identified with exact line citations and a proposed narrowest-safe resolution left for Cursor to confirm or override with STOP. Ready for kernel 'python backend/scripts/run_work_package.py start' once branch refactor/v5-canonical-activation-gate-1 is created/checked out."
}
