# ARCH-CONV-D — Independent Head of Architecture STOP A Decision

**Reference:** `ARCH-CONV-D-STOP-A-HOA-2026-07-30`  
**Work ID:** `ARCH-CONV-D`  
**Status:** APPROVED — PHASE 1 GOVERNANCE IMPLEMENTATION AUTHORISED  
**Runtime authority:** NOT AUTHORISED

## Decision

`MERGE_TO_SIGNAL_ALT_HIGH`

This is an identity decision, not a runtime merge.

## Governed identity relationship

- `signal_alt_high` is the sole canonical future ALT authority identity.
- `signal_hepatic_alt_context` is classified as a legacy predecessor/context implementation.
- It is not retained as a separately canonical medical family.
- It must not become a runtime alias because the two live implementations have non-identical signal behaviour.

## Legacy ownership

`signal_hepatic_alt_context` remains the temporary owner of `alt_hypotheses_v1.yaml` until ARCH-CONV-E explicitly ratifies and proves transfer, replacement or retirement.

ARCH-CONV-D must not change:

- signal activation;
- hardcoded thresholds;
- root-cause registry mappings;
- legacy WHY reachability;
- compiled WHY authority;
- emitted output.

## Successor requirements

ARCH-CONV-E must:

1. treat `signal_alt_high` as the only canonical ALT migration target;
2. perform bounded medical review of ALT frame roles;
3. remediate or replace the non-SSOT hardcoded threshold logic before retaining or transferring it;
4. decide and implement the hepatocellular collision-axis policy;
5. explicitly adjudicate legacy WHY transfer or retirement;
6. prove no accidental duplicate ALT user-facing authority.

## Axis decision

A future hepatocellular-injury axis is reserved and required for ARCH-CONV-E design review, but is not created or medically adjudicated by ARCH-CONV-D.

## Medical-review requirement

No Head of Medical Research review is required for this identity closure. The decision does not claim the two implementations are medically equivalent; it records canonical lineage while preserving both behaviours unchanged.

## Phase 1 boundary

Phase 1 may update only identity, predecessor/successor, inventory and architecture governance records.

No runtime, medical-authority, signal, threshold, loader, compiled artefact, legacy asset, collision resolver, frontend, AST, ALP/GGT or bilirubin change is authorised.
