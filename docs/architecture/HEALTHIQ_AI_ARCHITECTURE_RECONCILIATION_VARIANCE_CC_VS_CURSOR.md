# HealthIQ AI — Reconciliation Variance: Claude Code vs Cursor (ARCH-PROG-RECON-1-CC)

| Field | Value |
|---|---|
| **Work package** | ARCH-PROG-RECON-1-CC |
| **Compares** | Cursor's `HEALTHIQ_AI_ARCHITECTURE_PROGRAMME_RECONCILIATION.md` / `_OPEN_ARCHITECTURE_OBLIGATIONS.md` / `_ARCHITECTURE_CLOSURE_SEQUENCE.md` vs Claude Code's independently reconstructed `_CC` companions |
| **Method** | Independent first-pass findings (recorded in working note before this document was drafted) diffed against Cursor's conclusions after both were complete |
| **Overall read** | High substantive convergence — expected, since both audits read the same small set of primary authorities (ADR-RT-001–004, ARCH-RT-IDENTITY-PROV-1/C1, P3-LAYERB-INTEL-1, the current-state baseline). Convergence here is evidentiary agreement, not circularity. The material value of this second pass is in (a) sharper code-level citations, (b) one finding Cursor's evidence gestured at but did not pin down precisely, (c) one reachability finding neither document had previously made explicit, and (d) two unresolved count discrepancies flagged rather than silently adopted. |

---

## Comparison table

| Topic | Cursor conclusion | Claude Code independent conclusion | Verdict | Evidence | Impact on build sequence |
|---|---|---|---|---|---|
| **Remaining frame-collapse surfaces** | 4 remaining surfaces named generically in reconciliation §5 row B2 ("Jul audits still flag remaining family/first-match collapse on launch path consumers") and OBL-ARCH-001 evidence line ("e.g. interaction confidence keyed by `signal_id`") | 5 surfaces, named with exact file:line: the same 4 disclosed carry-forwards (`interpretation_display_layer_publish_v1.py`, `domain_score_assembler.py`, `narrative_report_compiler_v1.py`, `intervention_selector_v1.py`) **plus** `signal_interaction_builder.py`, which Cursor's own OBL-ARCH-001 evidence line names ("interaction confidence keyed by `signal_id`") but which is not counted among Cursor's stated "4 deferred" surfaces anywhere else, nor separated from the "5 fixed" set | **PARTIAL** — Cursor's evidence line is directionally correct and, on direct code read, fully confirmed (`node_ids` :63-66/145, `confidence_by_signal` :220-224 in `signal_interaction_builder.py`), but Cursor's own count (B2/OBL-ARCH-001) does not consistently reflect it as a 5th surface distinct from the "5 fixed" set claimed elsewhere in the same reconciliation | `signal_interaction_builder.py:63-66,145,220-224` — direct read, confirmed `activation_key` appears only as an output field (lines 150/155/245/250), never as a lookup key | PKG-1(-CC) scope must explicitly include this file's node/confidence logic, not treat it as already closed |
| **Provenance status** | OBL-ARCH-002 (explicit lineage, OPEN) + OBL-ARCH-004 (batch-JSON reachability, OPEN, explicitly states "`runtime_loaded: false` flags are not equivalent to load prevention") | Same conclusion, reached independently via direct code read of `signal_evaluator.py:33-36` (unconditional glob, no provenance filter) and `report_compiler_v1.py:817`/`signal_evaluator.py:528` (provenance_status carried as metadata only, never gates output) | **AGREE** | Both sides converge on substance; Claude Code's citations are lower-level (load-path + consume-path code) vs Cursor's higher-level policy-flag framing | No change — both support PKG-2(-CC) scope as written |
| **Blocked-cohort runtime reachability** | Stated as a fact (OBL-ARCH-004: "not equivalent to load prevention") without citing the specific load-path code or confirming which/how many `pkg_kb47_*` directories actually carry a loadable `signal_library.yaml` | Independently confirmed with a direct count: all 20 `pkg_kb47_*` directories carry a `signal_library.yaml`; traced the exact two call sites (load-side and consume-side) that establish reachability is unconditional | **AGREE**, Claude Code adds verification depth | `signal_evaluator.py:33-36`, `:528`; `report_compiler_v1.py:817` | Confirms PKG-2(-CC)'s reachability acceptance condition is the correct target, not a speculative one |
| **Dual WHY authority** | C1/OBL-ARCH-003: 1 compiled, 40 legacy YAML, 41 registry targets, dual path live, OPEN for launch-cohort retirement, DEFERRED_WITH_AUTHORITY as programme posture | Identical counts, reached independently: `compiled_hypothesis.py:17-18` frozenset cardinality 1; direct file count of 40 YAML; `root_cause_registry_v1.py:28-90` 41 tuples; confirmed mutual exclusivity at `root_cause_compiler_v1.py:539` | **AGREE** | Independently re-derived counts match exactly | No change |
| **Signal-generation interpretation** | §9 "Mixed legacy / current authorities map": package generations = "Mixed legacy/current (loaded)" — not framed as duplicate active libraries; §6 explicitly flags stale inventory docs (186/67 vs live 191/72... Claude Code independently found 192/72) | Same substantive conclusion (not duplicate active authorities; single compiled `signal_library.yaml` glob is the sole runtime-facing gate); independently flags the same stale-inventory problem | **AGREE**, with one unresolved sub-variance: Cursor states 191 active package directories; Claude Code's fork directly counted 192 directories (191 manifests, 1 directory without a manifest). Neither audit fully reconciled why the two counts differ by exactly one directory-vs-manifest unit | Fork direct count via shell (`ls knowledge_bus/packages | wc -l` = 192; manifest count = 191) | Does not change either closure sequence; flagged as a hygiene item (OBL-CC-005) rather than a build-blocking discrepancy |
| **Closure package count** | 3 (PKG-1 identity, PKG-2 provenance, PKG-3 WHY) | 3 (PKG-1-CC, PKG-2-CC, PKG-3-CC), same boundaries, reached independently from the sprint plan's own stated "do not combine" philosophy rather than from Cursor's document | **AGREE** | Both derive the same minimum from the same source constraint (day-one sprint plan §2) | No change — reinforces 3 as the floor |
| **Cursor PKG-1 (identity completion)** | Scope: "every launch-path consumer... uses `activation_key`"; acceptance condition references "Jul-audit-class collapse findings addressed" | Same scope, but Claude Code's PKG-1-CC explicitly names the interaction-builder gap as in-scope and adds a pre-work check (confirm live multi-frame exposure before sizing) that Cursor's PKG-1 does not include | **PARTIAL** — same package, Claude Code's version is more prescriptive about what "addressed" must mean at the code level | — | PKG-1-CC's acceptance condition is stricter; recommend using the CC version's explicit file list when this package is eventually authored |
| **Cursor PKG-2 (provenance/blocked-cohort honesty)** | Scope matches; STOP gate: "STOP if PSI wiring is smuggled in" | Same scope; Claude Code's PKG-2-CC adds an explicit STOP gate on the specific risk of silently dropping a currently-relied-upon Wave 1 signal if `BLOCKED` packages are excluded from firing without a launch-relevance review first | **AGREE**, Claude Code adds one STOP gate Cursor's version does not state | — | Recommend carrying the added STOP gate forward — excluding `BLOCKED` packages from the runtime load path is a live-behaviour change that could silently regress a Wave 1 finding, not just a provenance cleanup |
| **Cursor PKG-3 (WHY completion)** | Scope and STOP gates materially identical to Claude Code's PKG-3-CC | Same | **AGREE** | Both independently derive from ADR-RT-003 Decision 6's transition-stage table | No change |
| **Is legacy-active classification an acceptable end state?** | Reconciliation §11 / OBL-ARCH-003: "Temporary dual estate remains `DEFERRED_WITH_AUTHORITY` as programme posture, but launch-cohort retirement is still OPEN" — i.e., acceptable as *transitional*, not as *final* | Same conclusion, independently grounded in ADR-RT-003 Decision 4's own end-state language ("Single compile path; hand-editing YAML deprecated") and Decision 6 ("Immediate deletion of manual YAML or registry" explicitly "Rejected for day-one") | **AGREE** | ADR-RT-003 read directly by both; same textual anchor | No change |
| **Estate-indexed compiled card count** | States 10 compiled cards (reconciliation §8, current-state baseline cross-reference) | Fork evidence cites `active_intelligence_authority_manifest.md:42-44` stating 7 launch-included compiled cards + 1 hypothesis artefact | **UNVERIFIABLE / DISAGREE-UNRESOLVED** — this audit did not re-read `estate_index_v1.yaml` directly to adjudicate 7 vs 10; flagged rather than silently resolved in either direction | `active_intelligence_authority_manifest.md` (dated 2026-05-30, itself pre-dates ARCH-RT-IDENTITY-PROV-1 and P3-LAYERB-INTEL-1 — may simply be as stale as the package-generation inventory) | Does not change PKG-1/2/3(-CC) scope; recorded as OBL-CC-005 documentation-hygiene item requiring a direct `estate_index_v1.yaml` read before either number is cited as current |
| **Active signal families / activation keys / multi-frame families (139/197/51)** | States these as verified quantitative outputs (reconciliation §8) | Not independently re-derived — no fork re-ran a fresh `SignalRegistry` load and counted keys/families in this session | **UNVERIFIABLE** (by Claude Code) | — | These figures should be re-confirmed by a fresh run before being used to size PKG-1-CC's pre-work check (which asks precisely whether any live multi-frame family reaches the 5 collapse surfaces) |

---

## Findings on Cursor's own verification report (`ARCH-PROG-RECON-1_implementation_and_verification_report.md`)

Read as required evidence-base material (not as a "Cursor conclusion" subject to the independence rule, which applies specifically to the three reconciliation/obligations/closure documents). Two points worth carrying forward:

1. Cursor's own report discloses, in its own words, that "the single estate-wide active/dormant/blocked triad is UNVERIFIABLE because policy inventories disagree with runtime load behaviour." This is an honest, correct self-flagged limitation — and this audit's independent code read (§OBL-CC-003) confirms the underlying mechanism Cursor could not fully resolve: `SignalRegistry` loads unconditionally regardless of any policy inventory's active/dormant/blocked label.
2. A literal typo in Cursor's report header ("eature/arch-prog-recon-1-..." missing the leading "f") suggests the document was not proofread before being treated as an audit-of-record. Cosmetic, but worth a docs-hygiene note if this report is cited going forward.

---

## Summary verdict

No material disagreement was found on substance. The two audits converge on: registry-level identity is fixed; a bounded set of downstream consumers remain unfixed; provenance is honestly classified but not extracted, and — the sharpest independent contribution of this pass — not access-controlled either; WHY authority is deliberately, authorisedly dual; package generations coexist without competing as runtime authorities; and the correct minimum closure sequence is three packages in the stated order. The variances that exist are refinements of precision (one extra collapse surface with exact citations, one reachability mechanism traced to exact code, two unresolved count discrepancies flagged rather than adopted) rather than disagreements of conclusion. Both audits agree: no architecture-completion or beta-readiness declaration is warranted by this evidence.
